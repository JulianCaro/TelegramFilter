{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "python -c \"import os; print([k for k in os.environ if 'SESS' in k or 'API' in k])\" && python TelegramBot.py",
    "restartPolicyType": "ALWAYS"
  }
}